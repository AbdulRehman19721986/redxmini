# This repo does not run the application, so it has no real runtime env vars.
# This file documents the NAMES the private repo expects — set real values
# only in the private repo's own environment / each platform's dashboard.
# NEVER put real values in this file or commit a filled-in .env anywhere.

MONGO_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_KEY=
DATABASE_URL=
TENOR_API_KEY=
ADMIN_USERNAME=
ADMIN_PASSWORD=
OWNER_NUMBER=
BOT_NAME=
PREFIX=
BOT_MODE=
PORT=
